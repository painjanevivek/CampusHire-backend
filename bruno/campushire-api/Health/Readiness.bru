meta {
  name: Readiness
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/health/ready
  body: none
  auth: none
}

tests {
  test("readiness returns HTTP 200 when required dependencies are available", function () {
    expect(res.getStatus()).to.equal(200);
  });

  test("readiness reports the service as ok", function () {
    expect(res.getBody()).to.have.property("status", "ok");
  });
}
