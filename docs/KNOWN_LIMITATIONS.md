# Known Limitations Register

| ID | Limitation | Impact | Current control | Release owner / exit condition |
| --- | --- | --- | --- | --- |
| KL-001 | The credential-free parser container and abuse controls pass locally, but the exact policy is not yet reproduced on the selected managed staging launcher. | A provider/runtime mismatch could weaken the proven local boundary. | Minimal parser-only image, no network/credentials/input mounts, read-only root, capability/resource/time/output limits, cleanup and CI checks. | Platform security: attach managed-staging policy inspection and abuse evidence before pilot uploads. |
| KL-002 | Exhaustive Deep Security Scans are explicitly deferred. The active Frontend run was canceled on 2026-08-24 at the user's request, and the Backend run was not started. | No completed exhaustive report exists, and standard scans cannot prove exhaustive multi-agent coverage. | Separate standard scans, threat model, code tests, and the explicit deferral are recorded; no pass or no-findings result is claimed. | Security owner: resume only after the user lifts the deferral and a managed read-only worker profile is available, then run and accept independent Frontend and Backend Deep Scans. |
| KL-003 | A production-shaped local concurrency-20/worker baseline passes, but pilot volume, managed topology, SLOs, provider rates, and cost ceiling are not approved. | Local timings and incomplete rate inputs cannot be represented as production capacity or final cost. | Reproducible peak-load tooling, zero-error local evidence, provisional alerts, explicit labels, and a parameterized cost model prevent unsupported claims. | Product/platform owners: approve the envelope and repeat it on managed HTTPS with complete pricing. |
| KL-004 | Institutional retention periods, legal basis/consent treatment, export and appeal operations, incident contacts, audit readers, service ownership, and after-hours coverage are not approved. | Deletion holds, rights handling, escalation, and governance communications cannot be finalized. | Approval drafts, explicit retention holds, auditable overrides, implementation cross-check, pending owner/sign-off registers, and no invented contacts. | Institution privacy/legal, T&P, accessibility, product, and security/platform owners: complete and approve the registers. |
| KL-005 | Real Safari/macOS keyboard, screen-reader, student, and administrator UAT is not signed off. | Automated engines do not prove device-specific or representative-user usability. | A 126-check Chromium/Firefox/WebKit matrix covers axe, complete focus traversal, reflow, reduced motion, forced colors, and degraded states; the UAT pack defines the human boundary. | Accessibility/product owner: complete and sign the pilot sessions. |
| KL-006 | The complete Redis, worker, parser, ClamAV, private-storage, Gemini, and Qdrant failure matrix passes with synthetic controlled dependencies, but recovery is not yet exercised on selected managed vendors. | Provider-specific identity, recovery, quota, timeout, and network behaviour remains unknown. | Pinned service images, fail-closed/degraded paths, durable retries, duplicate-effect checks, parser cleanup, tenant checks, and AI-independent core operation are tested. | Platform owner: repeat the named matrix on managed staging and attach sanitized evidence. |
| KL-007 | Candidate and rollback images are locally content-addressed and archived but have not been promoted or signed in an approved private registry. | Local Docker identifiers cannot prove registry custody, deployment identity, or managed-environment provenance. | Exact Git archives, OCI digests, BuildKit attestations, hashed CycloneDX SBOMs, transfer-archive hashes, configuration/OpenAPI hashes, migration head, parser verification, and candidate/rollback smoke evidence are retained locally. | Platform/security owner: promote the exact archives, capture registry-qualified digests and approved provenance/signature evidence, then repeat managed smoke checks. |

No item in this register is silently waived. A release decision must close it or record an authorized, time-bounded risk acceptance with owner and expiry.
